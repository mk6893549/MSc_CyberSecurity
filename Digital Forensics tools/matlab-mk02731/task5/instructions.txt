Use LSB_steg_extract to find the plaintext of the stego image. 
Use the discovered code to unlock the next task.

This is a keyed steganography challenge. You must first determine whether
the key was used to encrypt the plaintext or whether it was used to
randomize the embedding path. Make sure to explain how you determined this
without having to brute force both options. 

Write matlab code to find the correct key by brute force. The key
has length 4 and has been randomly chosen out of all possible
combinations formed of the letters A, B, C, D.


key was used to encrypt the plaintext or whether it was used to
randomize the embedding path


C-9bc3-4c89-0141-e3f1-83d5-d654-Z