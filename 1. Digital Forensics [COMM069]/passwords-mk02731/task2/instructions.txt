
The password file has been encrypted with the gnu privacy guard (gpg)
and a 4-digit PIN code.  The encryption command was:

gpg --pinentry-mode loopback --batch --passphrase "(some PIN here)" --cipher-algo AES -ac -o password.enc password 

Brute force the 4-digit PIN code to decrypt the password file.

Use the John the Ripper tool to break the encryption password in the most
efficient way, i.e., using plausible password guesses only.

